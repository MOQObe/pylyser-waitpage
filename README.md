# Waiting page for Pylyser

## Setup
- Install Packages 'npm install'
- Use 'npm run dev' to run local server
- Use 'npm run build' to create a dist map, upload this to the server to update the waiting page